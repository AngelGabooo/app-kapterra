class AppConstants {
  static const String appName = 'KAAB TERRA';
  static const String slogan = 'Cultiva datos, cosecha valor';

  // URL Base de producción en Render
  static const String apiBaseUrl = 'https://kaabterra-api.onrender.com';

  // Duración de animaciones
  static const Duration splashDuration = Duration(seconds: 3);
  static const Duration fadeInDuration = Duration(seconds: 1);

  // Tamaños
  static const double logoSize = 160.0;
}